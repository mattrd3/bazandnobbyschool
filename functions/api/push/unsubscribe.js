import { json,readJson,ensurePushTable,verifyPlayer } from "../../../lib/push.js";
export async function onRequestPost({request,env}){const body=await readJson(request);if(!await verifyPlayer(env.DB,body.playerName,body.playerPin))return json({ok:false,error:"Player login could not be verified."},401);const endpoint=String(body.endpoint||'').trim();if(!endpoint)return json({ok:false,error:"Subscription endpoint is required."},400);await ensurePushTable(env.DB);await env.DB.prepare("DELETE FROM push_subscriptions WHERE endpoint=? AND playerName=?").bind(endpoint,String(body.playerName).trim()).run();return json({ok:true,subscribed:false})}
