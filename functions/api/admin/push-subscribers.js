import { json,ensurePushTable,verifyAdmin } from "../../../lib/push.js";
export async function onRequestGet({request,env}){const pin=new URL(request.url).searchParams.get('adminPin');if(!verifyAdmin(pin))return json({ok:false,error:"Incorrect admin PIN."},401);await ensurePushTable(env.DB);const result=await env.DB.prepare("SELECT playerName,COUNT(*) AS devices,MAX(updatedAt) AS updatedAt FROM push_subscriptions WHERE enabled=1 GROUP BY playerName ORDER BY playerName").all();return json({ok:true,players:(result.results||[]).map(r=>({name:r.playerName,devices:Number(r.devices||0),updatedAt:Number(r.updatedAt||0)}))})}
