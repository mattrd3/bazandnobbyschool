# bazandnobbyschool Cloudflare D1 v6 — short version label

Patch from v5.

Changed the app header status label from a long patch description to a short visible version label, e.g. `LIVE- v6`.

Replace `public/index.html`, `functions/api/[[path]].js`, and `test/run-tests.js` in GitHub. Do not overwrite `wrangler.toml`.
